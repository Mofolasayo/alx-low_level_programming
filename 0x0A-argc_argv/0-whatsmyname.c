#include "main.h"
#include <stdio.h>

/**
 *main - prints the name of the pprogram
 *@argc: Number of comand line arguments
 *@argv: array
 *Return: 0
 */

int main(int argc, char *argv[])
{
      printf("%s\n", argv[argc * 0]);
      return (0);
}
